# Server
HOST=0.0.0.0
PORT=1337
NODE_ENV=development

# Secrets - Generate random strings for production
# You can generate them with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Database - SQLite (Development)
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# Database - PostgreSQL (Production - Render)
# Uncomment and fill these for production:
# DATABASE_CLIENT=postgres
# DATABASE_URL=postgresql://user:password@host:5432/database
# DATABASE_SSL=true
# DATABASE_SSL_REJECT_UNAUTHORIZED=false

# Or use individual parameters:
# DATABASE_HOST=your-render-host.postgres.render.com
# DATABASE_PORT=5432
# DATABASE_NAME=your-database-name
# DATABASE_USERNAME=your-database-user
# DATABASE_PASSWORD=your-database-password
# DATABASE_SCHEMA=public
